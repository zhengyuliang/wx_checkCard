<!--pages/shop_select/shop_select.wxml-->
<!-- <i-panel title="group-水果"> -->
  <i-radio-group current="{{current}}" bindchange="handleFruitChange" >
    <i-radio wx:for="{{shopList}}" position="right" wx:key="{{item.shop_id}}" value='{{item.shop_name}}' data-index = "{{item.shop_id}}">
    </i-radio>
  </i-radio-group>
<!-- </i-panel> -->