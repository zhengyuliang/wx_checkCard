<!--pages/hotCategory/hotCategory.wxml-->
<view class="containers">
   <view class="selectDay">
      <Select
             bind:myget='getDate'
             >
      </Select>
   </view>
   <view class="payType" wx:if="{{!nodata}}" >
     <ec-canvas id="mychart-dom-line" canvas-id="mychart-line" ec="{{ec}}"></ec-canvas>
   </view>
   <view wx:if="{{nodata}}" class="nodata">暂无数据</view>
</view>
